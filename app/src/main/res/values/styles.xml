<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">-->

        <!-- Customize your theme here. -->
        <item name="colorPrimary">#66000000</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">#FF3030</item>
    </style>


    <style name="MyButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/green_dark</item>
        <item name="colorButtonNormal">@color/green</item>
        <item name="android:background">@drawable/edshape</item>
    </style>

    <style name="MyButton_fac" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/grayval</item>
        <item name="colorButtonNormal">@color/graymid</item>
        <item name="android:background">@drawable/edshape</item>
    </style>

    <style name="HeaderBar">
        <item name="android:background">#303F9F</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#46bdbf</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#f1f1f1
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#42221f1f
        </item>
    </style>

</resources>
